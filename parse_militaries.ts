import fs from 'fs';

// Try to grab the previous message payload that contains all the militaries data directly
// Since I can't read it from the messages directly, I need to use the fact that the user pasted it in.
// We saved it into a file called data_dump.txt just now? No, we didn't. 
